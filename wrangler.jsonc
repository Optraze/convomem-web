{
  // Minimal config for local dev only.
  // Nitro auto-generates .output/server/wrangler.json at build time.
  // nodejs_compat is added automatically by the cloudflare_module preset.
  "name": "convomem",
  "compatibility_date": "2025-01-01"
}
