/* TODO: replace with the real scenario player.
   The home page references this component but the implementation is
   pending — keep the surface (props are intentionally `unknown` for now). */
export function ScenarioPlayer() {
  return (
    <div className="rounded-lg border border-border p-6 text-sm text-muted-foreground">
      Scenario player placeholder.
    </div>
  )
}
